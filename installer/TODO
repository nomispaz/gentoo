#Frage: muss ich das als user ausführen oder geht das auch im chroot? Wie macht das Arch?
systemctl --user --now disable pulseaudio.service pulseaudio.socket
systemctl --user --now enable pipewire.socket pipewire-pulse.socket
systemctl --user --now disable pipewire-media-session.service
systemctl --user --force enable wireplumber.service

#Deutsche Tastatur in sddm

#compile-options optimieren

#Anpassungen in grub übernehmen
#upload angepasste hyperland.conf

#search for bin-packages
https://wiki.gentoo.org/wiki/Creating_an_ebuild_repository#Adding_an_ebuild_to_an_ebuild_repository
sudo equery -q list --portage-tree --full-regex '.*/.*-bin'


neue zsh config hochladen
